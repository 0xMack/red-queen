# docs

Design documents and write-ups that don't belong to a single `libs/`/`apps/`/`apis/`/`jobs/`
package — architecture decisions, use-case designs, and background on how pieces of the monorepo
fit together.

- `design/` — design documents, one per use-case or major component
